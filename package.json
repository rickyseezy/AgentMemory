{
  "name": "agentmemory-workspace",
  "private": true,
  "packageManager": "pnpm@11.12.0",
  "engines": {
    "node": ">=24.18.0 <25",
    "pnpm": "11.12.0"
  },
  "scripts": {
    "setup-ui:build": "pnpm --filter @agentmemory/setup-ui build",
    "setup-ui:check": "pnpm --filter @agentmemory/setup-ui check",
    "setup-ui:test": "pnpm --filter @agentmemory/setup-ui test:coverage"
  }
}
