# PF-001 implementation and release record

| Field | Value |
|---|---|
| Story | PF-001 — Install the complete local Brain from one MCP installation |
| Delivery status | In progress |
| Release status | Not releasable; all missing production capabilities fail closed |
| Current increment | Verified native package selection/transaction/postcondition, portable multi-agent MCP bootstrap, deterministic Claude MCPB/Gemini/generic host packages, production platform security adapters, executable seven-cell native support matrix, independently signed clean-host campaign verifier, hosted per-cell certification, byte-reproducible qualification, and 117-object copy-only immutable promotion |
| Current increment verification | The final local tree passes the complete race/shuffle suite, 80% per-package and changed-code launcher gates, architecture and package contracts, module integrity, vet, Staticcheck, strict lint, reachable-vulnerability scan, schema/workflow validation, and cross-compilation for macOS Intel, Linux x86-64/ARM64, and Windows x86-64; native macOS ARM64 passed the race suite. The new certification CLI, verifier, and release-workflow checker pass focused coverage at 91.4%, 82.0%, and 80.0%. Hosted final-commit, fuzz, and mutation evidence still must pass; external native signing and the actual clean-host campaign remain pending |

PF-001 is release-complete only when every acceptance criterion and mandatory test in
`TECHNICAL_REQUIREMENTS.md` is executable and passing against production artifacts on every
certified host. Passing unit tests for ports or policy does not prove that Docker, the Brain,
Neo4j, SQLite, models, or an agent integration can actually be installed.

This increment deliberately has no fake composition root and no adapter that can report Ready
from process existence, a filename, a Docker binary on `PATH`, or mocked service health. Missing
production inputs therefore keep the story in progress.

## Current release-completion delta

The implementation has advanced beyond several historical gap descriptions retained below. The
current shared tree now includes the runnable local Core, SQLite migrations, Neo4j integration,
three llama.cpp provider roles, hardened container images, protected secret projection, concrete
Core readiness/activation clients, native launcher factory, setup UI, supported agent document
formats, Windows Job Object execution, release-evidence verifiers, and an authenticated minimal
Ready MCP surface. The default Qwen embedding, reranking, and extraction artifacts are locked to
official revisions and exact digests. Release qualification downloads every source again, converts
the reranker twice with a digest-pinned network-disabled converter, compares both outputs, and live
tests all three roles in network-disabled containers.

PF-001 is still not release-complete for a pristine nontechnical user. The default native factory now
owns the retained release authority, protected first-start publishers, one Setup Retry supervisor, the
complete fourteen-phase graph, fresh operation-scoped runtime/product executors, native agent
configuration, and the portable host-package entry point. The repository now defines the closed
release-build -> host-package-build -> native-certification -> release-qualification ->
copy-only-promotion chain. The
remaining critical path is to supply the external signed/vendor inputs, execute that chain, prove
physical-capacity behavior on every certified native filesystem, certify custom-agent and
proxy/PAC/TLS-interception flows, and retain mutation, fuzz, hosted, clean-host, interruption,
accessibility, and no-egress evidence for the exact promoted bytes.

External release inputs remain separate blockers: Apple Developer ID/notarization authority,
Windows Authenticode authority, hardware-backed or approved release signing identity, runtime-vendor
terms and redistribution approvals, and the required pristine native hosts. Repository code and CI
must fail closed when any of those inputs are absent; tests cannot manufacture them.

### Portable MCP and host-package delivery

- `agentmemory-bootstrap` is a dedicated signed entry point that owns no stdout outside the MCP
  transport. It accepts only `mcp --agent <closed-host>`, returns stable privacy-safe stderr codes,
  starts one native package transaction, and exposes a protocol-valid status/cancel/open-setup
  surface before that transaction settles.
- The portable bridge adopts the installed, fixed-root native composition without proxying stdio.
  It preserves one monotonic status sequence across the temporary and installed applications,
  closes both lifecycles, and makes a repeated exact install idempotent by proving the installed
  launcher/helper/distribution postcondition before invoking the package manager.
- The portable layout is derived only from the real bootstrap executable under `bin/`; it rejects
  links, working-directory/PATH/environment authority, undeclared package formats, and ambiguous
  Linux distribution families. Publication and object signatures use distinct exact Sigstore SANs
  and the embedded official trusted-root/Rekor material.
- `agentmemory-host-package` verifies the publication signature, bootstrap signature, every selected
  native object/signature digest, exact version/source identity, and the closed target cell before
  writing anything. Claude emits MCPB v0.4 binary bundles; Gemini emits extension-directory ZIP
  transports with `${extensionPath}${/}`; generic emits the path-neutral custom-host contract.
  ZIP entries are sorted, fixed-time, mode-pinned, link-free, and reproducible.
- The completed archive is not permitted to contain its own digest. A detached canonical record
  binds archive, bootstrap, publication, publication signature, host, target, version, commit, and
  epoch. Host-package build signs publication first, assembles 15 target/host archives, then signs
  archives and records. Qualification reproduces every archive, re-verifies signatures, compares
  native package payloads to a freshly verified retained bundle, and promotion only copies the 117
  already-qualified files.

### Native certification and promotion evidence

- `contracts/pf001/support-matrix-v1.json` is the executable seven-cell authority for macOS Tahoe
  Intel/ARM on APFS, Ubuntu 24.04 x86-64/ARM64 on ext4, Fedora 44 x86-64/ARM64 on XFS, and Windows
  11 25H2 x86-64 on NTFS. It derives 54–58 no-skip trials per cell across installation, host
  integration, interruption, security, network, no-egress, upgrade/removal, usability, and
  accessibility scenarios.
- `check_pf001_certification` strictly validates the matrix; derives the GitHub job matrix and
  exact evidence paths; canonicalizes reports; derives the external key ID; assembles a canonical
  detached envelope from raw hardware-signer output without handling a private key; and verifies
  raw or deterministic ZIP campaigns. It rejects stale, waived, skipped, reordered, substituted,
  extra, missing, linked, oversized, noncanonical, wrong-release, wrong-matrix, wrong-key, and
  snapshot-reuse evidence.
- `pf001-native-certification.yml` requires an immutable campaign release bound to the final tag
  commit, uses GitHub release/asset verification, verifies every generated cell on hosted runners,
  reverifies the whole campaign, and produces exactly one hosted-attested certification ZIP.
- Release qualification accepts only the exact successful workflow/run/artifact identity, rejects
  self-hosted provenance for the hosted verification layer, repeats the independent signature and
  publication binding, and includes the ZIP as the 117th attested object. Promotion repeats the
  signature check before and after copy-only staging.
- The complete operator, evidence, signature, freshness, immutable-staging, and failure procedure
  is [`PF-001-NATIVE-CERTIFICATION.md`](PF-001-NATIVE-CERTIFICATION.md). Actual clean-host results
  and external keys remain release inputs; repository CI cannot manufacture them.

## Delivered in this increment

### Installation kernel and recovery

- A closed fourteen-phase `InstallApplication` saga implements VerifyHost through
  CommitActiveRelease with one narrow capability per phase.
- The aggregate binds operation ID, canonical plan digest, phase, attempt, verified artifacts,
  non-secret facts, runtime ownership, compensation boundary, next action, and reboot receipt.
- Save-before-side-effect and save-after-transition rules make the first unverified phase the
  only retry point. Re-entry validates the same plan and returns `AlreadyReady` only from a
  fully restored Ready aggregate.
- A cross-process host lock, versioned operation repository, HMAC-chained journal, and protected
  monotonic rollback anchor reject tamper, replay, plan substitution, skipped phases, duplicate
  completion, and ambiguous crash states.
- Active cancellation is a plan-bound mutation in the same authenticated operation aggregate and
  compare-and-swap authority as phase progress. `Cancel` does not wait for the machine mutation
  lock or claim terminal settlement; the running installer watches durable intent, cancels the
  phase context, persists `Cancelled`, performs owned cleanup with a fresh bounded context, and
  only then acknowledges the request. The short operation-state fence makes cancellation versus
  Ready a single linearizable decision across processes.
- Public application and phase errors use closed, privacy-safe codes; raw command output, paths,
  credentials, and platform diagnostics do not enter persisted step evidence.
- Reboot-required runtime transitions now register one owner-scoped, 24-hour continuation only
  after `RebootPending` is durable. The exact seven-field record contains no secret; it binds the
  launcher and operation journal hashes, operation ID, next action, expiry, and a CSPRNG nonce.
  macOS uses a per-user LaunchAgent, Windows uses the invoking user's `HKCU` Run value, and Linux
  uses the XDG autostart directory. Startup verifies the record, launcher, journal, machine/user,
  aggregate state, and canonical plan, atomically claims the token, persists `ResumeVerified`, and
  retains the login trigger until resumed progress is durable. Replay, expiry, substitution,
  partial cleanup, or stale terminal state fails closed and removes only the exact managed entry.

### Platform-protected bootstrap state

- Pristine start now writes a purpose-separated rollback-protected preparation intent before a
  plan, operation, or pointer can be published. It retains the exact verified template digest,
  operation/installation/generation/Brain/owner/agent-entry UUIDv7 identities, security epoch, and
  confirmed derived-plan digest. Concurrent launches select one winner; crashes and launcher
  restarts replay that exact authority instead of regenerating identities or falling forward to a
  newer template. The native identifier adapter now creates those identities from the operating
  system CSPRNG as RFC 9562 UUIDv7 values, binds the 48-bit Unix-millisecond timestamp, and fails
  closed on cancellation, invalid time, short entropy, or entropy-source failure.
- The first-start template adapter rechecks template kind, execution purpose, media type, platform,
  byte length, and digest and resolves durable preparations by exact digest without falling
  forward. The native composition now creates a fourth purpose-separated protected journal,
  resolves the invoking owner's certified agent configuration location, binds the native Docker
  endpoint and current launcher digest, and derives only the permitted per-host plan fields. A full
  canonical plan cannot be listed inside the signed release manifest that it embeds: doing so would
  create an impossible SHA-256 self-reference. Therefore the production source must be a separate
  signed bootstrap catalog binding both the release-manifest digest and template digest. The
  descriptor-rooted retained loader, signed outer-catalog verifier, inner product-manifest
  verifier, exact-digest replay, and no-fall-forward template source are now implemented. The
  production factory still needs its public release trust material and phase composition before
  first start can publish.
- Runtime-catalog acceptance now has its own purpose-separated rollback-protected journal and fixed
  authority namespace. The repository strictly decodes canonical closed snapshots, persists one
  monotonic high-water mark per catalog ID with optimistic CAS, confirms every write durably, and
  rejects stale writers, sequence regression, whole-journal rollback, tampering, unsafe permissions,
  non-canonical JSON, and any attempt to substitute an ordinary journal. Runtime-catalog anchors do
  not share the release-manifest anchor root or operation identifier.

- Linux uses owner-bound, no-symlink, regular-file paths, `0600` key material, atomic
  replacement, file/directory synchronization, and anchored replay protection.
- macOS binds the invoking user and device, rejects nontrivial ACLs, walks controlled paths
  without following symlinks, uses the Data Protection Keychain boundary for operation keys,
  and uses `F_FULLFSYNC`. A no-cgo build fails closed.
- Windows binds MachineGuid plus the invoking token SID. A random 256-bit operation key is
  user-DPAPI protected inside a non-roaming Credential Manager generic credential. Protected
  DACLs, owner-only mutexes, no-delete-share descriptor guards, no-reparse walks, hard-link/ADS/
  foreign-owner rejection, `ReplaceFileW` or write-through `MoveFileExW`, identity recheck, and
  `FlushFileBuffers` protect journal and anchor publication.
- Raw operation IDs never become filesystem components or Windows credential target names;
  fixed digest-encoded locators are used instead.

### Runtime policy and parent-phase integration

- The PF-006 runtime aggregate, exact runtime install plan, signed canonical runtime catalog,
  consent/terms bindings, prerequisite graph, ownership disposition, reboot receipt, and
  completion receipt are implemented as domain/application contracts.
- The release resource for a runtime catalog now has a strict canonical signed-envelope codec.
  The outer release-resource digest and inner detached-signature manifest digest are distinct,
  independently persisted evidence fields; runtime-plan derivation verifies both bindings and no
  longer assumes an impossible equality between a signed envelope and its embedded manifest.
- Runtime evidence requests now carry the immutable signed parent release and exact catalog-resource
  projection selected by the authenticated installation plan. The native release authority owns a
  production catalog loader that reruns the complete release verifier, requires the verified resource
  authorization, streams only the descriptor-rooted retained object, enforces its declared media
  contract, size, and SHA-256, and decodes the canonical signed envelope before catalog policy can run.
  A separate embedded runtime-catalog Ed25519 key-ID set and concrete canonical-byte signature
  verifier are now composed by the same release authority; release-manifest and host-policy keys
  cannot silently become runtime-catalog signing authority.
- The embedded trust document also carries a separate closed runtime-artifact publisher allowlist.
  Catalog verification accepts only an exact native verification mechanism, publisher identity,
  signing-key/certificate identity, and package/application identity tuple from that allowlist;
  every tuple now also resolves to an independently embedded exact SHA-256 certificate or publisher-
  key digest. The catalog cannot self-authorize its publisher, and byte-level platform-native
  verification is still mandatory before execution.
- Runtime evidence derivation now receives the exact signed parent host plan and its operation-bound
  successful host-evidence digest. A fail-closed projection adapter translates only those certified
  floors into runtime-catalog host vocabulary; it never claims more CPU, memory, or disk than the
  signed parent proved. macOS uses the documented leading Darwin major from opaque Apple build IDs
  as the product-defined numeric range key (`24F74` -> `24000`), while retaining the complete opaque
  build in the signed parent evidence; Windows uses its native numeric build and Linux uses a bounded
  major/minor/patch kernel key.
- The production runtime-evidence orchestrator now enforces the full trust sequence before a PF-006
  plan can be persisted: reverified retained outer envelope, certified parent-host projection,
  detached catalog signature, support window, source/redistribution policy, independent native
  publisher allowlist, catalog anti-rollback CAS, certified-runtime projection, and a separate native
  runtime observation receipt. It persists both the outer resource digest and inner signed-manifest
  digest and rejects any equality assumption, substitution, missing receipt, or contradictory digest.
- Runtime evidence is no longer an externally injected phase capability. The production graph derives
  it from the retained release authority, embedded runtime signature/publisher trust, protected catalog
  anti-rollback repository, exact re-attestation of the committed operation-scoped host receipt, and a
  build-tag-selected native observer. The macOS observer proves encrypted local storage, Hypervisor
  support, physical and available memory, exact owner-only Unix socket state, Docker application
  identity, and its embedded certificate digest. The Linux observer binds the non-root effective UID/GID,
  exact `/run/user/<uid>/docker.sock`, certified distribution/kernel/architecture, live resources,
  owner-controlled local storage, user namespaces, and SELinux policy while rejecting rootful sockets.
  The Windows observer binds Windows 11/amd64, live firmware-virtualization, memory and fixed-volume facts,
  the invoking user's `%LOCALAPPDATA%\Programs\DockerDesktop` application/executable paths, and
  `npipe:////./pipe/docker_engine`. Each backend
  reports a clean host only when every exact application, executable, and endpoint surface it owns is
  absent; inconsistent or unverified existing state is blocked. Exact compatible-runtime adoption remains
  blocked until the existing installation's native signer/package receipts and active capability evidence
  are joined to the same observation receipt.
- PF-006 execution no longer receives one planning-time runtime application as ambient long-lived
  authority. The operation-scoped plan query now reconstructs a `RuntimeExecutionAuthority` from the
  current authenticated parent aggregate, successful host receipt, signed outer catalog resource, and
  immutable persisted nested-plan record. On every process lifetime and retry, the native execution gate
  reloads that authority, re-verifies the outer release resource, inner detached catalog signature,
  native publisher policy, support window, and rollback anchor, and compares every platform,
  architecture, product, version, channel, sequence, terms, size, and catalog binding with the canonical
  PF-006 plan before building a platform application. The fourteen-phase graph now constructs this gate
  per operation; catalog authority cannot survive a crash only as trusted memory.
- Desktop catalog and execution authority now follow Docker's current documented consent and Windows
  installation contracts. macOS and Windows cells require AgentMemory's authenticated visible terms
  decision and forbid a contradictory mandatory vendor UI when the installer receives
  `--accept-license`. Windows requires the recommended per-user `--user` flow, derives every installed
  executable below the bound invoking user's local application directory, and leaves elevation only
  for host-proven WSL prerequisites.
- Linux catalog cells now bind the complete retained package set and its native repository trust
  chain rather than one aggregate artifact:
  the exact APT/DNF backend and version, official repository URL/suite/component, signing-key
  fingerprint and exact retained key artifact, canonical repository configuration, exact signed
  metadata/signature/package-index resources and their sizes/SHA-256/official URLs,
  all seven sorted native package identities/versions/sizes/SHA-256 values/official file URLs/native
  receipt digests and authenticating repository IDs, rootless service/tool evidence, active probe,
  capability policy, and visible terms. Each cell also binds the fixed package-owned Docker CLI and
  Compose plugin paths and byte digests. DNF cells additionally bind `/usr/bin/rpmkeys`, its byte
  digest, the exact distribution `rpm` package version, and an independent signed native package
  receipt; APT cells must leave every `rpmkeys` field empty. Every Linux cell binds `/usr/bin/pkexec`,
  its byte digest, exact owning package/version, and native receipt: `pkexec` for APT cells and
  `polkit` for DNF cells.
  projection. The schema rejects partial package sets, mutable versions, mirrors, source traversal,
  package/repository/prerequisite disagreement, capability disagreement, and Linux authority in a
  non-Linux cell. Distribution-owned prerequisites such as Ubuntu `uidmap` and Fedora
  `shadow-utils` are bound to a separate signed distribution repository; the Docker repository is
  not incorrectly treated as their publisher.
- A verified-catalog Linux authority projection now joins only that signed cell, the exact canonical
  PF-006 plan, and an independently observed non-root user/machine binding. The production resolver
  and native Linux binding adapter are implemented; plan/catalog/terms/host substitution fails
  closed. Unrelated workload inventory is read from the authenticated plan rather than guessed by
  the resolver, and the exact terms identity/version/official URL/presentation are bound into every
  consent request.
- The verified Linux catalog now projects the retained repository key, metadata, detached signature
  where applicable, package index, and all seven exact package files into the common hardened
  artifact protocol. The acquisition identity is the complete signed catalog digest, so a metadata
  snapshot substitution cannot reuse a package-set identity. Signed rollback and
  acquisition-safety headroom are part of the catalog and
  must reconcile exactly with the full capacity plan. The production adapter reserves exact
  complete trust-chain/package download slots before network access; the independent capacity application owns rollback
  and safety leases. It acquires only exact signed HTTPS objects through the ranged CAS application,
  revalidates every final digest/size/content key, retains the download reservation for the parent
  saga, and emits a versioned canonical retained-set proof. This unprivileged step explicitly does
  not claim native repository/package authentication.
- Native verification can reopen a final CAS object only through a descriptor-rooted streaming
  reader. It verifies the entire object before exposure, holds the store lifecycle lease while the
  consumer reads, requires an exact full read, hashes the consumed bytes, and rechecks file,
  directory, size, and digest identity on close. No verifier receives or reconstructs an ambient CAS
  path.
- The production APT trust adapter now verifies the retained public key and exact primary
  fingerprint, SHA-256/384/512 clear signature, trusted UTC validity, Release date and optional
  `Valid-Until`, Suite/Component/Architecture, signed SHA-256 and size of the selected package
  index, bounded gzip/xz/plain index decoding, and every exact package
  name/version/architecture/path/size/SHA-256 record. It authenticates both the Docker repository
  and the distribution repository before emitting verified evidence, and rejects duplicate,
  malformed, stale, future, substituted, truncated, or cross-repository data. The CAS reader is
  consumed for every package as part of the same proof. No GnuPG process or ambient keyring is used.
- Runtime identifiers, versions, source authorities, digests, publishers, host tuples, and
  prerequisite manifests use closed validation and canonical encoding.
- `EnsureContainerRuntime` binds the nested signed runtime plan to the parent PF-001 plan,
  accepts only aggregate-authenticated outcomes, carries a one-use verified reboot receipt,
  and maps administrator, cancellation, unsupported-host, conflict, retry, and completion
  states without manufacturing capabilities or ownership.
- The PF-006 aggregate now has a production authenticated repository with a purpose-separated
  protected journal and cross-process state fence. It strictly decodes canonical snapshots,
  restores the complete transition history, applies aggregate-version compare-and-swap, confirms
  durability on exact replay, and rejects stale, divergent, substituted, or malformed state.
- Before the first acquisition or host mutation, PF-006 now persists a separate authenticated
  `RuntimeOwnershipRecord` derived only from the canonical runtime plan, restored aggregate, and
  reverified platform authority. The record retains the exact runtime/product/channel, endpoint,
  native publisher and artifact bindings, sorted components/settings, pre-existing-state and
  consent digests, every mutation before/after digest, privilege receipts, and all reboot receipts,
  including consumed receipts. It advances monotonically in its own rollback-protected journal,
  finalizes only from the Ready aggregate, and is required by completion. The PF-001 parent output
  now binds a sealed completion receipt containing the finalized ownership-record digest; a Ready
  snapshot without that record cannot be projected or imported into the parent audit trail.
- Runtime cancellation is now a crash-safe child compensation saga. Cancellation first persists a
  pending compensation state and prepared ownership record, then re-resolves the exact signed
  platform authority, inspects the explicit runtime endpoint before and after releasing only the
  operation-owned CAS reservation/slot/partial identities, and accepts a receipt only when the
  runtime observation is unchanged. PF-001 acknowledges its cancellation intent only after that
  PF-006 receipt is durable in both the child aggregate and ownership journal. Missing children are
  proven no-ops; concurrently Ready runtimes and shared verified CAS objects are preserved.
- Certified macOS and Windows Docker Desktop execution now has a complete fail-closed application
  boundary: signed immutable desktop/helper authorities; native host, resource, filesystem, at-rest
  encryption, WSL-feature, and version evidence; exact retained installer verification; installed
  application publisher/version verification; direct signed Docker and Compose inspection; explicit
  consent and authenticated helper request/receipt contracts; native macOS Authorization Services
  and native Windows helper invocation; Windows UAC is limited to signed WSL prerequisite changes
  while the documented per-user Docker Desktop payload executes without elevation; application
  launch; reboot semantics; workload preservation;
  and an active Engine/Compose/bind/network/volume/loopback/local-execution probe with compensated
  cleanup. macOS uses Gatekeeper, Security.framework, notarization, signed Info.plist evidence, and
  owner-only no-follow workspaces. Windows uses BitLocker native evidence, WinVerifyTrust, exact
  external signer identity, fixed file-version evidence, protected DACL workspaces, DISM/WSL
  prerequisite evidence, and exact non-Windows-container installation policy.
- The signed runtime catalog now carries the closed desktop continuation required by those
  adapters: exact installer filename, distinct SHA-256 content bindings for the installed Docker
  CLI and Compose plugin, live-memory floor, digest-addressed capability probe and policy binding,
  independently signed rollback and acquisition-safety capacity, plus the Windows
  WSL version/features. The four desktop capacity values (download, installer expansion, rollback,
  and safety) must reconcile exactly with the signed reserve total. A verified-catalog projection joins that
  signed policy to the canonical PF-006 plan, independently embedded signer digest, and current
  native principal/machine binding. The production resolver repeats that join on every phase;
  native macOS and Windows binding adapters derive catalog-addressed owner-local artifact paths
  and fail closed on root, foreign, no-cgo, cross-platform, or malformed identity state.
- The verified desktop catalog now projects its one exact installer into the common hardened CAS
  protocol. The production acquirer reserves before network access, requires exact final
  digest/size/content-key evidence and a retained reservation, and then streams the descriptor-
  rooted verified CAS object into the catalog-addressed native cache. Materialization creates and
  re-verifies an owner-only private directory boundary, rejects links and path escapes, writes a
  private temporary file, verifies every consumed byte, publishes without replacement, syncs file
  and directory durability, and accepts an existing target only when its complete bytes still match
  authority. Native publisher verification remains a separate mandatory post-acquisition boundary;
  the Windows implementation now combines offline-constrained `WinVerifyTrust` chain/policy
  validation with an exact primary-signer certificate binding rather than accepting any otherwise
  trusted Authenticode publisher.
- Setup progress now restores the authenticated PF-006 child aggregate whenever PF-001 has reached
  `EnsureContainerRuntime`, folds the child revision into its monotonic public sequence, and maps
  every closed child pause/terminal state. Runtime consent is displayed only after the immutable
  operation-scoped nested authority is restored and its plan digest matches the child aggregate;
  terms digest, signed download/expanded sizes, platform reboot policy, and exact plan action are
  projected from that authority. Missing or substituted post-runtime child state fails closed.
- A process-local operation-bound decision hub now closes the setup UI/worker registration race
  without a background goroutine. The native setup factory binds it to the authenticated parent
  operation, and Accept/Decline are delivered to that hub instead of incorrectly restarting the
  already-running parent worker; cancellation and retry retain their separate authorities.
- Linux and Desktop consent receipts now expose a single canonical, purpose-separated signing
  statement containing every request, plan, authority, catalog/artifact/terms, principal, machine,
  nonce, confirmation, and validity binding but no signature bytes. A production protected signer
  authenticates those statements with HMAC-SHA-512 through the native owner/machine-bound operation
  key callback; it never retains key material and re-resolves the protected key for verification.
- The platform consent broker now turns the exact operation-bound hub decision into a signed Linux
  or Desktop receipt and independently authenticates fresh and restored receipts. The native
  composition creates separate consent-signing-key and consent-receipt roots, uses Keychain on
  macOS, user-scoped DPAPI on Windows, and the owner-only protected file-key boundary on Linux, and
  supplies one rollback-anchored strict receipt repository to both provisioners. Receipt publication
  is immutable and idempotent; operation, platform, plan, authority, principal, machine, nonce,
  terms, confirmation, expiry, canonical-byte, signature, or whole-journal substitution fails
  closed. Published signed native Desktop helper binaries, Windows WSL package acquisition on a pristine
  host, release credentials, and
  clean-host certification remain explicit blockers. Windows process creation is atomically
  assigned to its Job Object; unsupported or incomplete composition cannot report Ready.
- Linux privilege and Desktop mutation receipts now expose independent domain-separated
  authentication payloads. The Desktop Ed25519 authenticator copies only embedded public trust;
  Linux reloads the root-owned per-machine public key after helper execution and binds it to the
  exact release-authorized helper digest. Both reject cross-request substitution before signature
  verification and never accept a variable-length or foreign signature. A dedicated
  operation-scoped rollback-anchored replay journal atomically consumes Linux and Desktop nonces,
  rejects cross-domain reuse, survives process restart, and is purpose-separated from consent and
  progress state. The signed native release trust document requires the Desktop helper receipt
  public key plus a distinct native signing-certificate digest for every signed helper resource
  ID. The launcher resolves exactly one platform/architecture helper from the verified retained release,
  binds its immutable path and owner-private exchange directory, and re-verifies the release
  resource and exact certificate binding both when resolving authority and when checking the
  publisher. Supplying the protected Desktop private helper operation and release-time native
  certificate digests remains an external release input; Linux creates its distinct receipt key
  only inside the installed root-owned helper state.
- Windows Authenticode identity is no longer an abstract production port. A shared native security
  adapter retains the exact pathname without write/delete sharing, accepts only a binary embedded
  PKCS#7 signed object with exactly one primary signer, obtains that signer's issuer/serial
  `CERT_INFO`, locates the matching certificate in the returned store, and hashes the bounded exact
  DER bytes. Docker Desktop artifact/application verification and executable launch both compare
  that digest to immutable signed authority after `WinVerifyTrust`; executable authority now carries
  a mandatory platform-native publisher trust digest. Path identity is rechecked before the retained
  handle is released. Real signed-artifact execution remains a native certification requirement.
- The production DNF trust adapter now distinguishes package signing from repository-metadata
  signing exactly as DNF does. Docker's published detached `repomd.xml.asc` is mandatory and is
  checked against the catalog-pinned key/fingerprint; Fedora distribution metadata is instead
  pinned by the signed AgentMemory catalog because Fedora does not promise detached repository
  metadata signatures. In both modes the verifier checks exact `repomd.xml` SHA-256/size/location
  binding to `primary.xml.gz`, exact package NEVRA/architecture/path/size/SHA-256 records, and then
  authenticates every retained RPM with a publisher-verified `rpmkeys` executable, a private empty
  RPM key database containing only the catalog-pinned public key, and RPM's forced `all` verification
  policy. The signed schema rejects a metadata-authentication-policy/artifact mismatch, so a catalog
  cannot silently downgrade Docker or invent a Fedora metadata signature. This follows DNF's
  documented separation of `gpgcheck` and `repo_gpgcheck` and Fedora's published guarantee that each
  stable RPM is signed.
- Linux Docker, Compose, rootless setup, and DNF `rpmkeys` process authorities are now projected only
  from the signed Linux cell. Each authority is bound to the exact release manifest, runtime plan,
  path, bytes, root owner, closed executable role, package identity, and native receipt. The process
  verifier rejoins the opened executable evidence to that signed receipt on every execution; path or
  root ownership alone is never publisher evidence.
- The Polkit transport is a distinct signed executable role. It runs only `/usr/bin/pkexec` through
  the retained-object process adapter and only with `--disable-internal-agent`, the fixed
  `/usr/libexec/agentmemory/agentmemory-runtime-helper`, `--request-stdin`, bounded canonical stdin,
  and a scrubbed environment. Exit 126 is a user decline, exit 127 is an unavailable authorization
  surface, all private diagnostics are suppressed, and malformed/truncated receipts fail closed.
- The schema-v1 Linux helper codec now transports the exact retained signed release, signed runtime
  catalog, canonical PF-006 plan, catalog/helper resource IDs, and one typed nonce-bound request.
  It rejects duplicate/unknown fields and every noncanonical encoding, reconstructs request and
  expected-state digests only after independently projected Linux authority is supplied, and uses a
  separate canonical semantic receipt envelope whose signature remains an independent trust gate.
  The production Linux runtime factory now composes the verified catalog authority, package-bound
  Docker/Compose/rootless/`rpmkeys`/`pkexec` runners, artifact CAS and APT/DNF verifier, protected
  consent/replay repositories, Polkit broker, exact helper authenticator, active probes, and the
  fourteen-phase runtime application. The helper resource is selected by exact Linux architecture
  from the fully verified release; the signed catalog envelope bytes are rejoined to their release
  resource digest before elevation.
- The immutable Linux helper command now independently re-verifies the signed release, runtime
  catalog, host binding, exact helper identity, and retained artifacts as root before dispatching
  one of five closed transactions. It uses a root-owned per-machine receipt key, global execution
  lock, rollback-anchored pending/completed replay journal, idempotent state probes, and exact cached
  receipt replay. Debian/RPM package inputs install the fixed helper, fixed Polkit action, tmpfiles
  state declaration, and launcher symlink; a repository checker and pinned nFPM matrix reject
  widened authorization, unsafe modes, or package-schema drift. Clean-host package receipt,
  interruption, ownership/uninstall, and filesystem certification remain mandatory evidence.

### Release inventory and trust orchestration

- A strict canonical release manifest binds platform resources, immutable OCI references,
  provider/model roles, Compose topology, compatibility ranges, trust policy, SBOM and
  provenance associations, license/vulnerability policy snapshots, release history, and a
  monotonic release sequence.
- The release verifier requires signature, offline trust evidence, target/platform selection,
  protocol compatibility, every resource digest, OCI index binding, native publisher,
  CycloneDX and SPDX evidence, provenance, license, vulnerability, and anti-rollback checks.
- Concrete Ed25519 and certificate-transparency signature verification, offline Sigstore/Rekor/
  trusted-time/revocation verification, streaming SHA-256/size, CycloneDX/SPDX, SLSA provenance,
  signed qualification, OCI-index, native-publisher, and rollback-anchor adapters are present.
  The native release-trust stack now composes all of these gates with the retained signed
  distribution catalog and verified template source. Release builds can now embed strictly decoded
  public trust material, and the protected native release-sequence anchor is purpose-separated and
  production-composed. The final first-start factory and native launcher publisher policies are now
  composed from that authority. The release-generated production trust document and its owner-signed
  artifact set remain release blockers below. Embedded trust
  now carries a purpose-separated host-policy root set; the retained-release authority composes the
  detached host-plan verifier and build-tag-selected native probe without reusing the release-
  manifest signing domain.
- The top-level VerifyRelease phase advances only when the verified release identity, manifest
  digest, and sequence exactly equal the authenticated parent-plan projection.

### Artifact reservation and acquisition

- A signed artifact plan binds exact sources, chunks, offsets, sizes, digests, totals, rollback
  headroom, safety headroom, and content-addressed final keys.
- CAS storage accepts only descriptor-proven local filesystems: ext/XFS/Btrfs backed by a proven
  non-removable local block device on Linux, and `MNT_LOCAL` APFS/HFS on macOS. Reservation is
  narrower: only proven local ext/XFS currently qualifies. Btrfs is rejected because snapshots
  can invalidate NOCOW/preallocation guarantees, every Darwin filesystem including APFS/HFS is
  rejected until a snapshot-excluded dedicated allocation target can be proven, and Windows
  artifact reservation remains unsupported. Ambiguous backing storage always fails closed.
- The artifact store uses a durably published random marker plus filesystem identity, and every
  resume revalidates exact slot, transfer-pending, finalize-pending, or final physical layout and
  headroom. Revalidation observes and rejects drift; it never repairs untrusted state.
- HTTPS acquisition ignores ambient proxy authority, requires TLS 1.3, reauthorizes every
  redirect against the signed source list, requests exact ranges, rejects compression/chunked
  ambiguity, bounds reads, and verifies every chunk and final artifact digest. The signed
  acquisition plan now binds `system_proxy` or `direct_and_system_proxy`; production resolves
  every exact URL through macOS SystemConfiguration/CFNetwork (including bounded PAC execution),
  Windows WinHTTP (including per-user WPAD/PAC/manual/bypass policy), or Linux GIO. Proxy
  resolution, proxy-authentication, TLS-interception, and out-of-allowlist/captive-portal
  failures retain distinct privacy-safe error categories through acquisition. HTTP proxy
  credentials are never sent preemptively: one bounded Basic `407` challenge may request one
  ephemeral native credential and retry one exact CONNECT. Mutable credential bytes are
  destroyed immediately after the attempt. Linux GIO user-info and macOS CFNetwork proxy
  descriptor credentials are stripped from discovery and reread only after that challenge;
  Windows invokes the non-persisting native CredUI prompt without a Save control. A live TLS 1.3
  authenticated-CONNECT test proves the first request is credential-free and the second is
  exactly scoped, including after a signed cross-host redirect. Unsupported authentication
  schemes fail closed with the privacy-safe proxy-authentication category.
- Offline `bundle://` and online `https://` sources are routed only through an exact closed
  composite adapter. Partial and completed state is journaled, resumable, and published to the
  CAS without replacement.
- ReserveSpace and EnsureComposeBundle are integrated into the parent saga and cross-check the
  reservation, aggregate evidence, complete artifact set, and exact Compose artifact digest.
- Download slots remain separate CAS reservations. A distinct authenticated capacity aggregate
  journals per-artifact Expanded leases plus Rollback and Safety leases against an attested Docker
  backing pool, with a pending state before every physical consume, transfer, or release. Parent
  phases reserve these leases, consume each expanded artifact through an explicit materializer,
  transfer rollback/safety ownership at activation, and release only unconsumed operation-owned
  leases. The Docker adapter now physically reserves exact bytes, binds expanded leases to the
  signed source CAS digest/size, independently journals target consume/transfer/release intent,
  reconciles exact measured engine usage after crashes, and verifies retirement for cancellation,
  transfer, and uninstall. The signed release and install-plan schemas now bind an explicit target
  representation, target-storage identity, authority digest, and exact materialization projection.
  Compose-bundle and Docker image/volume target engines consume only those signed projections and
  retain their independent crash-recovery journals.

### Docker resource and Compose policy

- Signed Compose policy closes profiles, services, image digests, loopback ports, internal
  networks, volumes, labels, health probes, non-root users, read-only roots, capabilities, and
  `no-new-privileges`.
- Docker CLI and Compose are independent, direct executable authorities bound to the same signed
  release-manifest and runtime-plan digests, exact path/digest/owner/publisher, platform,
  architecture, and distinct roles. Desktop runner admission now cross-checks both tool digests,
  exact owner, native publisher identity, and closed platform policy against the verified desktop
  authority. Ambient `docker compose` plugin discovery is not used.
- Every capacity, managed-resource, migration, core/graph start, compensation, and cleanup call now
  reloads the authenticated parent operation, requires completed runtime-phase evidence, reconstructs
  `RuntimeExecutionAuthority` from the immutable plan repository, re-verifies the outer release and
  inner runtime catalog, and creates fresh Docker/Compose runners for the exact local endpoint. The
  verified release inventory is carried through the catalog envelope, so no planning-time path or
  hand-built executable capability can survive a restart.
- The runner holds and revalidates the authorized executable object, requires platform-native
  publisher policy, uses exact argv, a scrubbed environment, explicit local endpoint, bounded
  output, and no command shell. macOS uses Security.framework designated-requirement and
  Gatekeeper assessment, Linux requires an authenticated package receipt, and Windows uses
  WinVerifyTrust plus an exact signer-identity dependency. Unix cancellation supervises the
  complete process group through bounded TERM/KILL. Windows uses
  `PROC_THREAD_ATTRIBUTE_JOB_LIST` to attach the child to its Job Object atomically at creation.
- Managed network and volume reconciliation rejects foreign ownership, duplicate/ambiguous JSON,
  unsafe names, remote contexts, and destructive replacement. Creation is idempotent and
  journaled with a complete inventory digest.
- Compose rendering/config validation/up/wait/probe/down operations are digest-bound and use
  only the signed topology. Compose input and secret materialization is guarded by retained
  descriptor/handle ancestry; the Unix generation directory is host-owner-only while its read-only
  secret leaf remains readable by container UID/GID 10001. There is no Docker socket mount into a
  container.

### Agent configuration

- Host-neutral JSON merge policy preserves unrelated fields, rejects duplicate keys and
  ambiguous ownership, adds one marker-bound AgentMemory entry, and requires the prior managed
  entry digest before replacement.
- The application performs detect/read/validate/plan/compare-and-swap/verify and restores the
  exact backup after a failed invocation when restoration remains conflict-safe.
- Linux, macOS, and Windows filesystem stores implement owner-bound, no-link/reparse, atomic,
  durable, receipt-bound, replay-aware compare-and-swap and conflict-safe restore behavior using
  their platform-specific ACL/DACL and publication primitives. Native certification remains
  outstanding; the production location resolver now selects the documented user-scope Codex
  (`~/.codex/config.toml`), Claude Code (`~/.claude.json`), and Gemini CLI
  (`~/.gemini/settings.json`) files, plus Cursor's global (`~/.cursor/mcp.json`) file. The published
  `agentmemory.custom-agent-registration.v1` contract lets any other MCP stdio host own its own
  registration and invoke `mcp --agent custom`; AgentMemory then records a deterministic
  handshake-bound `verify_custom` result without calling a configuration store or guessing a host
  path. Generic JSON locations remain adapter-supplied rather than guessed, and “GLM” model
  selection uses the concrete host through which it runs. The production phase now
  re-verifies the complete signed release, selects exactly one platform launcher descriptor and
  digest, reconstructs the native publisher-bound process runner, merges only the plan-bound
  location/entry, and executes the signed-launcher MCP handshake. Real Codex, Claude, and Gemini
  native certification remains required.

The host-location projection is based on the vendors' current user-scope contracts:
[OpenAI Codex MCP configuration](https://developers.openai.com/codex/mcp/),
[Anthropic Claude Code MCP scopes](https://code.claude.com/docs/en/mcp), and
[Google Gemini CLI settings](https://codelabs.developers.google.com/gemini-cli-hands-on).
Release certification must revalidate these contracts against every pinned supported host version;
the URLs are design evidence, not a substitute for the signed compatibility manifest.

The custom verifier follows the official MCP
[stdio transport](https://modelcontextprotocol.io/specification/2025-11-25/basic/transports),
[lifecycle](https://modelcontextprotocol.io/specification/2025-11-25/basic/lifecycle), and
[tools](https://modelcontextprotocol.io/specification/2025-11-25/server/tools) contracts. Its
publisher-bound conversation runner waits for the `initialize` response before sending
`notifications/initialized`, gates `tools/list` on that transition, bounds every line and the
complete output, closes stdin for graceful shutdown, and settles the entire child process tree on
timeout, cancellation, malformed framing, output overflow, or premature exit.

The native Docker endpoint projection follows Docker's documented local transports: a macOS
unprivileged install can use the owner socket at `~/.docker/run/docker.sock`, Windows uses
`npipe:////./pipe/docker_engine`, and the Engine default on Linux is
`unix:///var/run/docker.sock`. See Docker's
[macOS permission requirements](https://docs.docker.com/desktop/setup/install/mac-permission-requirements/)
and [Docker CLI socket reference](https://docs.docker.com/reference/cli/docker/). The runtime phase
must still attest the live endpoint and must not trust this routing projection as runtime evidence.

### Readiness and activation

- The readiness application requires all eleven independent probes: SQLite integrity, migration
  head, writable volumes, graph compatibility, key access, audit append, deletion guard, expired
  lease recovery, local providers, semantic write/index/recall, and default-egress denial.
- A readiness receipt exists only after every exact probe passes and is bound to operation, plan,
  release, generation, manifest, Compose digest, and evaluation time.
- Active-release policy enforces monotonic release/resource/security generations, persists host
  and Core projections transactionally, reconciles bounded crash states, and retains the prior
  generation for rollback.
- These applications do not substitute for concrete Core/SQLite/Neo4j/provider probes or a Core
  activation endpoint.

### Quality enforcement

- ADR-001 through ADR-018 are accepted and the architecture checker enforces inward dependencies,
  layer-specific standard-library allowlists, and test imports.
- CI pins Go, actions, Staticcheck, govulncheck, golangci-lint, and Mutago versions.
- Required gates include formatting, module integrity, vet, Staticcheck, vulnerability scanning,
  strict lint, race/shuffle tests, an 80% per-package floor, 80% changed-code coverage, changed
  mutation MSI/covered-MSI of 80%, and a nightly full mutation gate.
- Every declared parser/security fuzz target runs as an independent parallel CI matrix, with
  longer scheduled campaigns.
- CI defines hosted unit-test jobs for macOS Intel/ARM, Linux x86_64/ARM64, and Windows x86_64;
  a configured job is not certification evidence until the exact commit passes it.

## PF-001 acceptance status

| Acceptance area | Current evidence | Release status |
|---|---|---|
| Pristine host provisions a certified local runtime without technical steps | The operation-scoped native provisioners, portable MCP bootstrap, exact package transaction/postcondition, setup surface, reboot continuation, ownership/cancellation compensation, removal flow, executable support matrix, signed-campaign verifier, and hosted qualification chain are production-composed. No owner-signed release or independently signed pristine-host campaign has run | Blocked only on external signed artifacts, approvals, hosts, and executed certification evidence |
| Signed AgentMemory stack reaches Ready | The default native factory now owns the retained verified release, one first-start/Retry supervisor, the complete fourteen-phase graph, runnable Core/provider stack, migrations, capacity/resources/Compose, readiness, activation, and authenticated Ready handoff | Blocked on exact signed production bundle and native product E2E evidence |
| Interrupted/repeated install is safe and idempotent | Kernel, protected journals, active-cancellation CAS/fence, preparation intent, reboot receipts, artifact/capacity/expanded-target journals, exact Docker reservation, and target lifecycle fault tests exist | Partial; certified kill/power/logout/reboot matrices outstanding |
| Default local models perform semantic index/recall with no egress | Pinned official model sources, reproducible qualification, three local provider roles, and the live readiness contract exist | Partial; exact signed release payload and clean-host no-egress evidence outstanding |
| Unsupported/unsafe conditions remain NotReady and preserve prior state | Closed outcomes, compensation, ownership, source/path/trust/resource policies cover implemented boundaries | Partial; live platform and product E2E evidence outstanding |
| Offline bundle passes identical trust/readiness gates | Offline artifact source, shared verification policy, native package selector, and host packages all consume the same signed publication/object evidence | Blocked on an owner-signed production bundle and clean-host no-egress execution |
| Agent configuration is preserved automatically | Portable policy/application, Codex TOML, Claude/Gemini/Cursor-compatible JSON, exact documented host-location resolution, release-bound launcher selection, native publisher verification, signed-launcher handshake, Linux/macOS/Windows stores, path-neutral custom contract, MCPB, Gemini extension, and generic package manifests are production-composed | Partial; real host/marketplace certification outstanding |
| Certified OS/architecture matrix passes | The exact seven-cell matrix, 54–58 no-skip trial sets, evidence schemas/paths, independent Ed25519 authority, immutable campaign handoff, hosted per-cell reverification, and promotion gates are executable and fail closed | Blocked on execution against owner-supplied pristine hosts and production-signed bytes |

## Release blocker ledger

### Repository-owned production work

Closed in the current tree: `NewNativeFactory` now invokes the verified production first-start
composer; the resulting release authority and sole supervisor are owned by the returned MCP
lifecycle. The graph builder supplies every one of the fourteen phases. After authenticated runtime
completion, product capacity, managed-resource, and Compose applications are rebuilt per operation
call from the reverified release inventory and runtime catalog. Agent configuration is built from
the exact canonical plan, release-verified launcher resource, platform publisher policy, protected
store, and MCP invocation verifier. Missing packaged trust or any partial capability still fails
before pointer publication or host mutation.

Remaining work:

1. Execute the implemented immutable multi-platform release chain with real inputs: digest-pinned
   images and Compose lock, publisher-signed native launchers/helpers/installers/bootstrap binaries,
   qualified models/tokenizers, schemas/migrations, SBOMs, provenance, policy reports, offline
   Sigstore/trusted-time/revocation material, and the complete offline bundle. Repository policy now
   reproduces, signs, qualifies, and copy-promotes one manifest-bound set; no real set exists yet.
2. Execute the reviewed no-skip campaign for the completed native managed-runtime removal path on every supported pristine host. The signed catalog-to-authority projection,
   authenticated nested-consent progress projection, operation-bound local decision transport,
   protected platform receipt signing/authentication, and rollback-anchored receipt repository
   exist; the macOS/Windows child saga now composes authenticated consent, artifact, mutation,
   terms, launch, and capability phases. The Linux child saga now includes exact retained APT/DNF
   authentication, closed root helper execution, package/rootless/user-service mutation, and active
   capability verification. Microsoft-signed offline WSL MSI and `.wsl` distribution acquisition,
   feature enablement, exact version-2 distribution verification, and owner-scoped one-use
   macOS/Windows/Linux reboot/sign-in continuation are implemented. A purpose-separated,
   authenticated and rollback-protected ownership journal now records pre-mutation state and
   finalizes into the sealed parent audit receipt. Cancellation now durably releases only exact
   operation-owned reservation/partial state after unchanged-runtime proof and before the parent
   acknowledges cancellation. `RemoveManagedRuntime` is now production-composed through two MCP
   steps: a durable non-destructive plan using finalized ownership and an exhaustive dependency
   scan, followed by a non-preselected impact-specific decision bound to the exact operation and
   plan digest. Execution repeats the exhaustive scan, invokes only the closed authenticated
   platform removal transaction, verifies exact absence, and persists the terminal receipt.
   The executable campaign now requires this flow without a skip; Docker's documented production
   package/installer paths remain mandatory and convenience scripts remain banned.
3. Execute the `physical-capacity-reservation` trial on APFS, ext4, XFS, and NTFS for artifact
   storage/reservation and every expanded-target engine. Cross-compilation and unit tests are not
   physical-allocation evidence. No platform may substitute logical free space for physically
   retained bytes.
4. Execute the declared native agent-registration and proxy/PAC/authentication trials on
   pristine platform hosts. Codex, Claude, Gemini, and Cursor use their certified user-scope
   locations. Custom hosts use the published path-neutral stdio contract, and GLM selects the
   concrete host through which it runs. No guessed path is permitted.
   The bounded Basic challenge/retry transport, Linux GIO credential bridge, macOS CFNetwork
   descriptor bridge, non-persisting Windows CredUI broker, and live TLS 1.3 authenticated-CONNECT
   tests are implemented. No proxy credential is accepted from environment, plan, journal,
   arguments, or MCP input; every unsupported authentication scheme fails closed.

The current native-runtime design is checked against Docker's documented
[macOS command-line installer and proxy/PAC flags](https://docs.docker.com/desktop/setup/install/mac-install/),
[Windows installer flow](https://docs.docker.com/desktop/setup/install/windows-install/), and
[Ubuntu Engine repository installation](https://docs.docker.com/engine/install/ubuntu/). The
Windows offline continuation must follow Microsoft's documented
[offline WSL MSI and `.wsl` package flow](https://learn.microsoft.com/en-us/windows/wsl/install#offline-install),
with exact signed artifacts in the release inventory; these references do not authorize mutable
downloads at installation time.

Native setup proxy resolution follows Apple's documented
[`SCDynamicStoreCopyProxies`](https://developer.apple.com/documentation/systemconfiguration/scdynamicstorecopyproxies%28_%3A%29)
and [`CFNetworkCopyProxiesForURL`](https://developer.apple.com/documentation/cfnetwork/cfnetworkcopyproxiesforurl%28_%3A_%3A%29),
Microsoft's documented
[`WinHttpGetIEProxyConfigForCurrentUser`](https://learn.microsoft.com/en-us/windows/win32/api/winhttp/nf-winhttp-winhttpgetieproxyconfigforcurrentuser)
and per-URL [`WinHttpGetProxyForUrl`](https://learn.microsoft.com/en-us/windows/win32/api/winhttp/nf-winhttp-winhttpgetproxyforurl),
and GIO's documented
[`GProxyResolver.lookup`](https://docs.gtk.org/gio/vfunc.ProxyResolver.lookup.html). Cursor uses its
documented global [`~/.cursor/mcp.json`](https://docs.cursor.com/context/model-context-protocol)
contract; the project-local `.cursor/mcp.json` is never modified by installation.
Challenge handling follows Go's documented dynamic
[`GetProxyConnectHeader`](https://pkg.go.dev/net/http#Transport) boundary. The remaining native
certification is checked against Apple's documented proxy descriptor keys and Microsoft's
non-persisting
[`CredUIPromptForWindowsCredentials`](https://learn.microsoft.com/en-us/windows/win32/api/wincred/nf-wincred-creduipromptforwindowscredentialsw)
contract. The portable acquisition transport intentionally supports challenge-scoped
Basic authentication only; it does not claim SSPI/NTLM/Negotiate support.

Native continuation registration follows Apple's documented
[per-user LaunchAgent domain](https://developer.apple.com/library/archive/documentation/MacOSX/Conceptual/BPSystemStartup/Chapters/CreatinglaunchdJobs.html),
Microsoft's documented
[Run/RunOnce registry contract](https://learn.microsoft.com/en-us/windows/win32/setupapi/run-and-runonce-registry-keys),
and the freedesktop.org
[XDG autostart specification](https://specifications.freedesktop.org/autostart/0.5/). Windows uses
the invoking user's persistent `Run` value rather than `RunOnce`: the launcher itself provides
authenticated one-use consumption and exact cleanup, while Microsoft documents standard-user and
logon reliability caveats for `RunOnce`.

Runtime removal follows Docker's documented
[Docker Desktop uninstall contract](https://docs.docker.com/desktop/uninstall/) and
[Ubuntu Engine package-removal contract](https://docs.docker.com/engine/install/ubuntu/#uninstall-docker-engine),
but those vendor commands are never implicit product-uninstall actions. Docker explicitly warns
that Desktop removal destroys local Docker data, while Engine packages and Docker data directories
are separate removal decisions; AgentMemory therefore requires its own complete dependency scan,
ownership proof, impact-specific confirmation, and fail-closed removal plan before invoking any
closed platform removal transaction.

The consent-key design is checked against [Apple Keychain Services](https://developer.apple.com/documentation/security/keychain-services),
Microsoft's user/machine-scoped [CryptProtectData](https://learn.microsoft.com/en-us/windows/win32/api/dpapi/nf-dpapi-cryptprotectdata)
and integrity-checking [CryptUnprotectData](https://learn.microsoft.com/en-us/windows/win32/api/dpapi/nf-dpapi-cryptunprotectdata),
the Linux kernel [Key Retention Service](https://www.kernel.org/doc/html/latest/security/keys/core.html),
[`persistent-keyring(7)`](https://man7.org/linux/man-pages/man7/persistent-keyring.7.html), and the
[Secret Service specification](https://specifications.freedesktop.org/secret-service/latest/).
Kernel persistent keyrings can expire and do not provide the required durable reboot boundary;
Secret Service can be absent on headless systems and therefore cannot be the mandatory Linux root.
The Linux file-key path remains permitted only after the existing owner, no-link, permission, and
encrypted-local-storage attestations pass.

Docker's [Desktop license documentation](https://docs.docker.com/subscription/desktop-license/)
states that the Docker Desktop agreement applies to Docker Desktop on macOS and Windows while Docker
Engine/Moby licensing is unchanged. Consequently the separate authority-and-entitlement
confirmation is Desktop-only. Docker documents the standalone Engine as an open-source project in
the [Docker Engine documentation](https://docs.docker.com/engine/) and distinguishes applicable
open-source component licenses in its [Terms of Service](https://www.docker.com/legal/docker-terms-use/).
The canonical PF-006 plan now carries the exact signed terms ID, version, HTTPS URL, digest, and
presentation policy. Construction and decode reject a Desktop agreement in a Linux cell, an Engine
license disclosure in a Desktop cell, foreign presentation modes, and substituted legal URLs.
Setup projects the authenticated Engine disclosure on Linux and the Docker Subscription Service
Agreement only on macOS/Windows. Release artifacts that retain the old cross-platform substitution
remain rejected by the catalog and plan boundaries.
5. Raise and retain all mandatory evidence gates for the exact commit: 80% per-package and 80%
   changed-code coverage, changed/full mutation thresholds, every fuzz target, release qualification,
   the 54–58-trial native clean-host matrix, interruption/power-loss campaigns, and novice
   accessibility tests. The campaign verifier rejects every skip, waiver, stale result, extra or
   missing attachment, reused snapshot, and wrong-release binding.

### External authority and certification evidence

The exact owner-supplied inputs, official vendor requirements, and fail-closed
release decision are maintained in
[`PF-001-EXTERNAL-RELEASE-INPUTS.md`](PF-001-EXTERNAL-RELEASE-INPUTS.md).

1. Apple Developer ID, hardened-runtime entitlements, Keychain access group, signing,
   notarization, and signed-host Keychain certification.
2. Windows Authenticode release credentials and native execution certification on the declared
   clean NTFS cell, including real signed-artifact leaf-certificate matching, Credential
   Manager, DPAPI, DACL, reparse, crash, logout, reboot, and power-loss campaigns. The concrete
   `WinVerifyTrust` plus exact signer-extraction adapter is repository-owned and implemented; the
   production certificate/private operation and clean-host evidence are external inputs.
3. Runtime-vendor terms, stable installer metadata, publisher identities, redistribution rights,
   and any offline redistribution approval. AgentMemory cannot grant or infer these rights.
4. Signed production release trust roots, transparency evidence, revocation material, SBOMs,
   provenance, license decisions, vulnerability reports, and model redistribution licenses.
5. Pristine no-runtime VMs/hardware for every certified OS/architecture, novice usability and
   accessibility studies, packet capture proving default no-egress behavior, hostile path-swap
   campaigns, interruption at every side-effect checkpoint, and sustained soak/upgrade/rollback/
   uninstall certification.

These inputs cannot be truthfully generated by unit tests or placeholder files. Until they are
available and pass their gates, `CommitActiveRelease -> Ready` must remain unreachable in a
production binary and PF-001 remains not releasable.

## TDD record

- **Red:** compile-failing and behavior-failing tests defined the fourteen-phase state machine,
  every transition and interruption boundary, active cancellation-versus-Ready ordering and
  watcher cleanup, journal/anchor tamper and replay, platform owner/ACL/path rules, runtime catalog
  and consent bindings, release trust matrix, acquisition range/digest/capacity rules, executable
  authority/publisher/process-tree behavior, Docker ownership/topology, agent configuration
  compare-and-swap/restore, readiness, and activation reconciliation.
- **Green:** the minimum domain policies, application use cases, narrow ports, durable adapters,
  and parent-phase integrations were implemented without a permissive fallback or fake Ready path.
- **Refactor:** canonicalization, immutable copies, typed error mapping, ownership validation,
  content-addressed identities, nil-capability rejection, and shared safe output construction were
  centralized while keeping platform effects behind adapters.

Every later blocker-closing increment requires the same Red-Green-Refactor evidence and its listed
unit, property, fuzz, integration, E2E, security, recovery, and platform tests. The mandatory 80%
coverage floor is not a substitute for testing security-relevant decisions.

## Security, compatibility, and rollback notes

- Bootstrap secrets, provider credentials, source contents, raw process output, and user paths are
  excluded from install evidence. Secret-bearing inputs are represented by protected references.
- The Linux owner-only file fallback does not claim protection from the invoking principal or a
  privileged actor restoring an entire old key/journal/anchor set.
- The macOS Keychain integration test is opt-in because it requires the production signed host,
  entitlement, stable application identity, and unlocked invoking-user Data Protection Keychain.
  A skip is never certification evidence.
- Windows test binaries cross-compile on macOS, but cross-compilation is not native Windows proof.
  The privilege-dependent symlink fixture may skip when the native runner lacks that privilege;
  such a skip is not certification evidence.
- Journal and snapshot DTOs are versioned and reject unknown versions/fields. A future migration
  must be signed, forward-only, and tested, or retain the previous reader for rollback.
- Production composition order is lock -> owner/key binding -> digest locator -> authenticated
  journal -> protected rollback anchor -> operation repository/application -> unlock. Returning an
  undecorated journal or releasing the lock before final persistence violates the design.
- Upgrades may activate only after shadow verification and must retain the prior generation.
  Uninstall never removes a reused runtime and may remove an AgentMemory-provisioned runtime only
  after a second consent and proof that no foreign workload depends on it.

## Current automated evidence

The last complete local checkpoint passes formatting, module integrity, architecture, vet,
Staticcheck, pinned golangci-lint, govulncheck, randomized race, per-package 80%, and changed-code
80% gates. The consolidated atomic launcher profile records 85.6% statement coverage and passes
the repository's exact per-package and changed-code checker; the launcher infrastructure package
also passes its focused 80% floor. Linux amd64/arm64, Windows amd64/arm64, and macOS amd64/arm64
changed packages cross-compile without executing foreign binaries. The new host-package command
and domain pass focused coverage at 80.4% and 91.1% and are included in that consolidated run.
Hosted evidence must still run for the final commit. The final staged commit and hosted CI evidence
are still pending, and no fuzz-count, mutation-score, hosted-matrix, or
native-certification claim is made here. Cross-compilation is not native Windows, Linux, or macOS
certification.

The required thresholds remain 80% statement coverage per Go launcher package, 80% changed-code
coverage, and 80% changed mutation MSI and covered-MSI. The resulting profiles, machine-readable
mutation report, fuzz matrix, and CI links must be attached to this record before the increment can
claim those gates passed. Native platform campaigns remain mandatory regardless of portable or
cross-compiled coverage.

## Verification commands

Run from the repository root:

```text
gofmt -l apps/launcher tools
go mod tidy -diff
go mod verify
go vet ./apps/launcher/... ./tools/...
staticcheck ./apps/launcher/... ./tools/...
govulncheck ./apps/launcher/... ./tools/...
golangci-lint run ./apps/launcher/... ./tools/...
go run ./tools/check_launcher_architecture -root . -include-tests
mkdir -p build
go test -race -shuffle=on -count=1 -covermode=atomic \
  -coverprofile=build/go-cover.out ./apps/launcher/... ./tools/...
go run ./tools/check_go_coverage -profile build/go-cover.out -root . \
  -base origin/main -package 80 -changed 80
```

CI runs every fuzz function declared under `apps/launcher` and enforces both changed-code and
nightly full-package mutation scores. A native supported Windows host must additionally retain race
and coverage profiles for `windowssecurity`, `bootstrap`, and `filesystem`; macOS certification must
retain the signed-host Keychain/ACL/durability campaign; Linux certification must retain supported
filesystem, rootless runtime, package-signature, privilege, reboot, and power-loss evidence.

## Status rule

Do not change Delivery status to Done or Release status to Releasable until every row in the
acceptance table is Complete, every repository-owned blocker is closed with production code, every
external evidence item is attached to the release, all required artifacts are signed and pinned,
and the complete certified-host test matrix passes without an unapproved skip.
